// Jest global setup for backend tests
// Increase timeout for async operations
jest.setTimeout(30000);

beforeAll(async () => {
  // Place global initialization logic here, e.g. database connection
});

afterAll(async () => {
  // Clean up resources, close connections, etc.
});
